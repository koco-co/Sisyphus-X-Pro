# 🚀 快速开始: 无人值守 AI 开发流程

这是一个基于 Anthropic 最新研究的 **长时间运行 AI Agent** 开发框架,专为 Sisyphus-X-Pro 项目构建。

## ⚡ 3 分钟快速启动

### 对于 AI Agent (Claude Code)

```bash
# 一键启动 (推荐)
source .claude/harness/quickstart.sh
```

这个命令会自动:
1. ✅ 启动所有开发服务
2. ✅ 运行健康检查
3. ✅ 显示当前进度
4. ✅ 提示下一步操作

### 对于人类开发者

```bash
# 启动开发环境
source .claude/harness/init.sh

# 查看项目状态
python .claude/harness/test_helper.py

# 停止所有服务
source .claude/harness/stop.sh
```

## 📊 当前项目状态

```
📊 功能完成状态:
==================================================
总计: 54 个功能
已完成: 0 个
完成率: 0.0%

🔄 用户认证模块 (FR-001): 0/8
🔄 首页仪表盘 (FR-002): 0/3
🔄 项目管理 (FR-003): 0/6
🔄 关键字配置 (FR-004): 0/5
🔄 接口定义 (FR-005): 0/6
🔄 场景编排 (FR-006): 0/7
🔄 测试计划 (FR-007): 0/6
🔄 测试报告 (FR-008): 0/5
🔄 全局参数 (FR-010): 0/4

📋 下一个待实现功能:
  - AUTH-001: 用户可以通过邮箱和密码注册新账户
```

## 🎯 AI Agent 工作流程

### 第一步: 了解当前状态

```bash
# 1. 启动环境
source .claude/harness/quickstart.sh

# 2. 阅读进度日志
cat .claude/harness/claude-progress.txt

# 3. 查看功能清单
cat .claude/harness/feature_list.json | python -m json.tool | less

# 4. 查看最近的提交
git log --oneline -20
```

### 第二步: 选择并实现功能

```bash
# 查看待完成功能 (按优先级排序)
python .claude/harness/test_helper.py

# 选择下一个功能: AUTH-001
# 阅读 .claude/harness/feature_list.json 中 AUTH-001 的详细说明
```

**功能实现步骤**:
1. ✅ 阅读功能的 `description` 和 `steps`
2. ✅ 编写单元测试 (pytest)
3. ✅ 实现代码逻辑
4. ✅ 编写 E2E 测试 (Playwright)
5. ✅ 验证所有测试通过
6. ✅ 更新 `feature_list.json` 中 `passes: true`

### 第三步: 提交并记录

```bash
# 代码检查
ruff check backend/ --fix
pyright backend/
cd frontend && npm run lint && cd ..

# 提交变更
git add .
git commit -m "feat: 实现用户注册功能 (AUTH-001)

- 添加 User 模型和数据库表
- 实现注册 API 端点
- 创建注册页面组件
- 编写单元测试和 E2E 测试

测试结果:
- 单元测试: ✅ 全部通过
- E2E 测试: ✅ 全部通过
- 代码覆盖率: 95%

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

git push origin main
```

```bash
# 更新进度日志
vim .claude/harness/claude-progress.txt
# 添加新的会话记录
```

## 📁 核心文件说明

### 必读文件

| 文件 | 说明 | 何时阅读 |
|------|------|----------|
| `quickstart.sh` | 一键启动脚本 | 每次会话开始 |
| `feature_list.json` | 54 个功能的详细清单 | 选择功能时 |
| `claude-progress.txt` | 开发进度日志 | 了解项目状态 |
| `coding_agent_prompt.md` | Coding Agent 工作指南 | 不清楚流程时 |
| `session_checklist.md` | 会话检查清单 | 确保工作质量 |

### 参考文件

| 文件 | 说明 |
|------|------|
| `README.md` | Harness 系统详细文档 |
| `SUMMARY.md` | 实施总结和技术细节 |
| `initializer_agent_prompt.md` | Initializer Agent 提示词 |
| `init.sh` | 环境初始化脚本 |
| `stop.sh` | 停止服务脚本 |
| `health_check.py` | 健康检查脚本 |
| `test_helper.py` | 测试辅助工具 |

## ⚠️ 重要约束

### ❌ 禁止的行为
1. **一次实现多个功能** - 每次会话只实现一个
2. **跳过测试** - 未通过测试不能标记完成
3. **修改功能定义** - 只能修改 `passes` 字段
4. **留下半成品** - 会话结束必须代码干净
5. **硬编码配置** - 所有配置通过环境变量

### ✅ 必须遵守
1. **TDD 开发** - 先写测试,再实现代码
2. **中文注释** - 所有注释使用中文
3. **代码规范** - 遵循 ruff/pyright/ESLint
4. **错误处理** - 完整的错误处理
5. **端到端测试** - 必须通过 Playwright 验证

## 🔍 故障排查

### 环境问题
```bash
# 重新初始化环境
source .claude/harness/init.sh

# 查看服务日志
tail -f logs/backend.log
tail -f logs/frontend.log

# 检查 Docker 服务
docker-compose ps
```

### 测试失败
```bash
# 查看详细错误
pytest backend/tests/ -v -s

# 运行单个测试
pytest backend/tests/test_auth.py::test_register -v

# E2E 测试
npx playwright test --headed
```

### 代码检查失败
```bash
# 自动修复
ruff check backend/ --fix

# 类型检查
pyright backend/
```

## 📞 获取帮助

### 查看文档
```bash
# Harness 系统文档
cat .claude/harness/README.md

# 实施总结
cat .claude/harness/SUMMARY.md

# 工作指南
cat .claude/harness/coding_agent_prompt.md
```

### 检查清单
```bash
# 会话检查清单
cat .claude/harness/session_checklist.md
```

## 🎓 学习资源

1. **Anthropic 研究论文**: [Effective harnesses for long-running agents](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents)
2. **项目开发指南**: `CLAUDE.md`
3. **项目说明文档**: `README.md`

## 🚀 下一步

1. 运行 `source .claude/harness/quickstart.sh` 启动环境
2. 阅读 `.claude/harness/coding_agent_prompt.md` 了解工作流程
3. 查看 `.claude/harness/feature_list.json` 选择要实现的功能
4. 开始第一个功能的开发: **AUTH-001 (用户注册)**

---

**记住**: 这个系统的目标是让 AI Agent 能够在多个上下文窗口中持续、可靠地工作。遵循流程,保持代码质量,记录进展,为下一个会话铺平道路。

**作者**: poco
**版本**: 1.0.0
**日期**: 2026-02-13
